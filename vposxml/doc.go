// Package vposxml implements Cardlink VPOS XML API 2.1 over HTTPS: request signing with
// Canonical XML 1.0 + SHA-256 digest, response digest verification, and typed helpers for
// capture, status, refund, cancel, IRIS QR sale, and recurring operations.
package vposxml
